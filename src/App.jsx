import Scene from "./scenes/Scene";
export default function App() {
    return (
        <div style={{maxWidth: "100vw", maxHeight: "100vh", height: "100vh"}}>
            <Scene/>
        </div>
    );
}